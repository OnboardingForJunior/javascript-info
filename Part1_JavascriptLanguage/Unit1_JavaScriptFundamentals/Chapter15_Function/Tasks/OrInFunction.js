// 논리연산자 사용하기
function showMessage(text) {
  text = text || '빈 문자열';
  return `${text}`;
}
// undefined

function showMessage2(text) {
  text = text || '빈 문자열';
  return `${text}`;
}
showMessage2("새로운 문자열"); // 새로운 문자열